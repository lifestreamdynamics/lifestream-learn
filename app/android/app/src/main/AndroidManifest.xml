<!--
  Slice P7b — WebAuthn / passkeys deployment note.

  Production Credential Manager integration binds the app to the RP
  domain via Digital Asset Links. Before shipping passkeys to real
  users, host a file at

    https://<RP-domain>/.well-known/assetlinks.json

  matching this app's signing-cert SHA-256 fingerprint. Typical payload:

    [{
      "relation": ["delegate_permission/common.handle_all_urls",
                   "delegate_permission/common.get_login_creds"],
      "target":   { "namespace": "android_app",
                    "package_name": "com.lifestream.learn.lifestream_learn_app",
                    "sha256_cert_fingerprints": ["<SHA-256 in UPPERCASE COLON form>"] }
    }]

  Derive the fingerprint for the debug keystore with:

    keytool -list -v -keystore ~/.android/debug.keystore \
      -alias androiddebugkey -storepass android | grep SHA256

  Dev / emulator flow works without the file because the RP-ID is
  "localhost" and the plugin uses the
  `android:apk-key-hash:<base64url(raw SHA-256)>` synthetic origin —
  no web round-trip needed. The `WEBAUTHN_ORIGIN` CSV on the API side
  must include both `http://localhost:3011` (host side) AND the
  apk-key-hash origin (client side).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:label="Lifestream Learn"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:enableOnBackInvokedCallback="true">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
        <!-- url_launcher needs to probe for an Activity that can handle
             `https` ACTION_VIEW intents. Without this block Android 11+
             returns `canLaunchUrl == false` for web URLs, breaking
             Privacy Policy / Terms links in Settings. -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>
</manifest>
